apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: togglemaster
  labels:
    app: analytics-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
    spec:
      containers:
      - name: analytics-service
        image: 236604670467.dkr.ecr.us-east-2.amazonaws.com/analytics-service:c23433687d8baeaa271826bf9701995b192074d7
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        env:
        - name: DYNAMODB_TABLE
          value: "ToggleMasterAnalytics"
        
        - name: SQS_QUEUE_URL
          value: "https://sqs.us-east-2.amazonaws.com/236604670467/analytics-queue"
        
        - name: AWS_REGION
          value: "us-east-2"

        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "200m"
            memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: togglemaster
spec:
  type: ClusterIP
  selector:
    app: analytics-service
  ports:
  - port: 80
    targetPort: 5000